---
- hosts: localhost
  collections:
    - community.postgresql
    - community.general
    - jfrog.platform
  roles:
    - postgres

- hosts: localhost

  collections:
    - community.general
    - jfrog.platform
  serial:
    - 1
    - 100%
  roles:
    - role: artifactory
      when: artifactory_enabled | bool

- hosts: localhost
  collections:
    - community.general
    - jfrog.platform
  roles:
    - role: xray
      when: xray_enabled | bool
